spring:
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///test_customer
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

eureka:
  client:
    enabled: false

logging:
  level:
    root: WARN
    com.bank.customerservice: DEBUG
